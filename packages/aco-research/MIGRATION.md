# ACO Research Migration

S6 introduces `@archon/aco-research` as an evidence-consumption package only.

It models existing Graphify evidence, graph waiver closure, upstream manifest snapshots, and Agentic Search reports. It does not run Graphify, fetch network data, execute CLI commands, write graph caches, or write artifact bundles.
