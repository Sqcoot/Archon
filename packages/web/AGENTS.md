# Web Package Instructions

Purpose: React/Vite frontend, routing, Zustand stores, API client, and UI components.

- Import workflow/API types from `@/lib/api` generated types, not server packages.
- Keep operational UI dense, clear, and consistent with existing design.
- Validate type-check, lint, and relevant UI tests after changes.
- Regenerate API types only when server OpenAPI schema changes.
