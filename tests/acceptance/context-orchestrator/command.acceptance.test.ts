import { describe, test } from 'bun:test';

describe('ACO slash command acceptance', () => {
  test.todo(
    'Spec: 011-command-contract.md Acceptance: ACO-COMMAND-001 slash command surface is deferred from CLI MVP'
  );
});
